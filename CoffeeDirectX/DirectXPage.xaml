<Page
    x:Class="CoffeeDirectX.DirectXPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoffeeDirectX"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <SwapChainPanel x:Name="swapChainPanel" >
        <Grid.ColumnDefinitions>
        <ColumnDefinition Width="70"/>
        <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Button 
            x:Name="backButton" Margin="12,22,0,0" Style="{StaticResource NavigationBackButtonNormalStyle}" 
            VerticalAlignment="Top" AutomationProperties.AutomationId="BackButton" Foreground="White"/>

        <TextBlock 
            x:Name="pageTitle" Foreground="White"
            Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" TextWrapping="NoWrap" VerticalAlignment="Top" Margin="-6,10,0,40"
            Text="DirectX" IsHitTestVisible="False" Width="280" HorizontalAlignment="Left" AutomationProperties.AutomationId="CsPageTitle"/>
        
    </SwapChainPanel>

</Page>
