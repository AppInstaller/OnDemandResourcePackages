<UserControl
    x:Class="CoffeeUniversal.Controls.PackageDeploymentControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoffeeUniversal.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Width="300">

    <Grid HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="8*" />
        </Grid.ColumnDefinitions>

        <Image Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" Margin="10,5" VerticalAlignment="Center" x:Name="PackageLogo" Height="60" Width="60"/>
        <TextBlock Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Text="PACKAGE NAME GOES HERE" x:Name="PackageName" />
        <ProgressBar Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" x:Name="downloadProgressBar" Value="50" Margin="10,5" HorizontalAlignment="Stretch" VerticalAlignment="Center" />

    </Grid>
</UserControl>
